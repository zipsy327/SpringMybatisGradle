   create table sawondb (
       num smallint auto_increment primary key,
       sawonname varchar(20),
       photo varchar(50),
       gender varchar(10),
       hp varchar(20),
       buseo varchar(20),
       ipsaday varchar(20),
       writeday datetime);